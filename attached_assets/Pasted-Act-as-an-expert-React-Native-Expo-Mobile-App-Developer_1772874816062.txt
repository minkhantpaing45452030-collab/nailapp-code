Act as an expert React Native/Expo Mobile App Developer. Build a fully functional, production-ready (100% error-free) mobile application for local storage only. Do not use external cloud databases; use AsyncStorage or SQLite to ensure all data persists permanently on the user's local device.

**1. BRANDING & UI/UX DESIGN:**
- Theme: "Luxury, Cute, and Sweet".
- Global Header: At the top of the app, display the main title "Nail By Yoon" and a subtitle below it "@make with love from Mg". Both texts must be beautifully styled in an elegant, cute typography using a Pastel Pink color scheme.
- Colors: Soft pastel pinks, clean white/off-white backgrounds, with soft luxury shadows.
- Buttons: All primary action buttons must be "Stylish 3D Floating Action Buttons (FAB)" featuring a '+' icon. 

**2. CORE ARCHITECTURE & NAVIGATION:**
Implement bottom tab navigation or a clean side menu containing:
- Home (Calendar Dashboard)
- Savings Vault (Piggy Bank)
- Monthly Archive (History)

**3. SCREEN 1: CALENDAR DASHBOARD (HOME)**
- Top Dashboard Cards (Calculated dynamically for the current month):
  * Total Monthly Income (Sum of all days).
  * Total Monthly Expense (Must be displayed in striking RED color), placed below the income.
  * Net Profit (Income - Expense), placed next to the Expense.
- Interactive Calendar: Display a full month calendar showing Day, Date, and Month.
- On-Click Date Action: Tapping any date opens a "Daily Entry Modal/Page".

**4. DAILY ENTRY MODAL (When a date is clicked):**
- Input 1: Total Income for that specific date.
- Input 2: Total Expense for that specific date.
- Expense Tags: Include selectable tags for the expense input: "Food" (မုန့်စား), "Shop Expenses" (ဆိုင်အသုံးစရိတ်), "Shop Rent" (ဆိုင်လခ), and "Staff Salary" (၀န်ထမ်းလခ).
- Camera/Receipt Feature: Include a button to open the device camera or gallery. The user can take a photo of physical records/receipts and save it specifically attached to this date.

**5. SCREEN 2: SAVINGS VAULT (PIGGY BANK)**
- Accessible via a dedicated button (Piggy Bank icon).
- Display a large, beautiful card showing the "Total Accumulated Savings".
- Include an input field and a 3D '+' FAB for the user to add new savings amounts. Any amount entered here adds to the total savings pool.

**6. SCREEN 3: MONTHLY ARCHIVE (HISTORY)**
- A page listing all previously completed and saved months.
- Save Month Action: Users can click a "Save/Archive Month" button on the calendar to lock and save that specific month's data into this archive.
- Viewing Archive: Clicking on any saved month opens a detailed list showing the daily income, expenses, tags, and photos day-by-day for that entire month.

**7. TECHNICAL CONSTRAINTS:**
- State Management: Handle complex calculations seamlessly without UI lag.
- Image Storage: Save captured images locally using the device's file system and link their URIs to the specific date's local database entry.
- Ensure thorough error handling (e.g., empty inputs, camera permissions). The app must be fully usable as a standalone local application with zero crashes.